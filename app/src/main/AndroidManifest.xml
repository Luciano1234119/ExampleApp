<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.exampleapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExampleApp"
        tools:targetApi="31">

        <!-- Activity principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            tools:ignore="WrongManifestParent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de Login -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />

        <!-- Activity de Registro -->
        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="false" />

        <!-- Activity de Bienvenida -->
        <activity
            android:name=".ui.welcome.WelcomeActivity"
            android:exported="false" />

        <!-- Activity del Gráfico -->
        <activity
            android:name=".ui.graph.GasGraphActivity"
            android:exported="false" />

        <!-- Activity de Prueba de Alarma -->
        <activity
            android:name=".ui.alarm.AlarmTestActivity"
            android:exported="false" />

        <!-- Configuración de Firebase -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/purple_500" />

    </application>

</manifest>
